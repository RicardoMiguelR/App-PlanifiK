doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        style
            include ./email.css
            
    body
        h2 Confirma tu cuenta Planifi-K
        p Ya estas casi inscrito, solo da click en el siguiente botón para finalizar el proceso.
        a(href=`${confirmarUrl}`) Confirmar cuenta
        p Si no puedes acceder a este enlace, visita #{confirmarUrl}.