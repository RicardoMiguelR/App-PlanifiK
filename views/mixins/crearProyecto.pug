mixin crearProyecto(proyecto = {})

    if locals.mensajes
        .alertas-proyectos
            each categoria in Object.keys(locals.mensajes)
                each error in mensajes[categoria]
                    .alerta-proyectos(class=`${categoria}`)
                        p= error

    form.agregar-proyecto(action=`/nuevo-proyecto/${proyecto.id || ''}` method="POST")
        .campo
            h4(for="nombre") Nombre proyecto:
            input(value=proyecto.nombre type="text" id="nombre" name="nombre" placeholder="Nombre de proyecto")
        .campo.enviar
            input(type="submit" value=`${proyecto.nombre ? 'Guardar' : 'Agregar'}` class="boton")